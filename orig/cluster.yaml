apiVersion: wohlig.com/v1alpha1
kind: CompositeCluster
metadata:
  name: team-a
  labels:
    cluster-owner: sohel
spec:
  id: team-a
  compositionRef:
    name: cluster-aws
  parameters:
    # Possible values small, medium, large
    nodeSize: medium
    version: "1.26"
    # minNodeCount: 1