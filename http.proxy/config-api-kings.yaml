kind: HTTPProxy
apiVersion: projectcontour.io/v1
metadata:
  name: combinedaggregator
spec:
  routes:
    - conditions:
        - prefix: /
        - header:
            name: x-key-id
      rateLimitPolicy:
        global:
          descriptors:
            - entries:
                - remoteAddress: {}
      services:
        - name: combinedaggregator
          port: 80
  virtualhost:
    fqdn: combinedaggregator.playexch.co
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*" # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - "*" # allows any origin
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m" # preflight requests can be cached for 10 minutes.