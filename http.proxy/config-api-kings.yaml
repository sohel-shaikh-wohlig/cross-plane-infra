kind: HTTPProxy
apiVersion: projectcontour.io/v1
metadata:
  name: archive-cron
spec:
  routes:
    - conditions:
        - prefix: /
      rateLimitPolicy:
        global:
          descriptors:
            - entries:
                - remoteAddress: {}
      services:
        - name: archive-cron-cmlevel
          port: 80
  virtualhost:
    fqdn: archive-cron.playexch.co
    corsPolicy:
      allowCredentials: true
      allowOrigin:
        - "*" # allows any origin
      allowMethods:
        - GET
        - POST
        - OPTIONS
      allowHeaders:
        - "*" # allows any origin
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: "10m" # preflight requests can be cached for 10 minutes.