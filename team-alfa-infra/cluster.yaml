apiVersion: wohlig.com/v1alpha1
kind: CompositeCluster
metadata:
  name: team-alfa
  labels:
    cluster-owner: sohel
spec:
  id: team-alfa
  compositionRef:
    name: cluster-aws
  parameters:
    # Possible values small, medium, large
    nodeSize: small
    # version: "1.20"
    # minNodeCount: 1